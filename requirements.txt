# Railway/Production requirements
# Incluindo base.txt diretamente para compatibilidade com Railway

# Django
django>=4.2,<5.0
django-environ>=0.11.2

# Django REST Framework
djangorestframework>=3.15.0
djangorestframework-simplejwt>=5.3.0
drf-spectacular>=0.27.0

# CORS
django-cors-headers>=4.3.0

# Database
psycopg2-binary>=2.9.9

# Celery
celery>=5.4.0
redis>=5.0.0
django-celery-beat>=2.6.0

# Security
argon2-cffi>=23.1.0
django-encrypted-model-fields>=0.6.5
# Blog (Story 1.1) — DEVE espelhar requirements/base.txt. Este arquivo é uma
# cópia manual inline pro Railway; se adicionar dep em base.txt, replicar AQUI
# ou o deploy crasha (ModuleNotFoundError).
bleach>=6.0
django-csp>=3.7,<4.0
django-ratelimit>=4.1

# Logging
python-json-logger>=2.0.7

# AI / LLM
anthropic>=0.40.0

# Email
django-anymail[resend]>=11.0

# Production
whitenoise>=6.7.0
gunicorn>=23.0.0
sentry-sdk[django,celery]>=2.0.0
